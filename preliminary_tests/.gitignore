# ── Python virtual environments ────────────────────────────────────────────────
.venv/
venv/
env/
source/           # second venv created during early setup

# ── Python cache & compiled files ──────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so

# ── Environment / secrets ──────────────────────────────────────────────────────
.env
.env.*
*.env
**/*.env

# ── macOS system files ─────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ── Jupyter notebooks ─────────────────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb

# ── Intermediate / scratch output files ───────────────────────────────────────
# Keep only the final versioned output (output5.csv), not scratch versions
output.csv
output.json
output2.csv
output3.csv
output4.csv
# output5.csv  ← intentionally NOT ignored; it is the final pipeline output

# ── Test / temp output files ──────────────────────────────────────────────────
test_output.csv

# ── Large downloaded corpora (not reproducible from code, use Git LFS later) ──
data_prep/downloaded/

# ── Editor / IDE ──────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*~

# ── OS temp ───────────────────────────────────────────────────────────────────
Thumbs.db
